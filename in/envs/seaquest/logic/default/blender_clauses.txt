neural_agent(X):-close_by_enemy(P,E).
neural_agent(X):-close_by_missile(P,M).
neural_agent(X):-many_enemies(X).
logic_agent(X):-visible_diver(D).
logic_agent(X):-oxygen_low(B).
logic_agent(X):-full_divers(X).